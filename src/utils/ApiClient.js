import { http } from './Utils';

// this should have all api endpoints consumed to easily track them

export const createUser = async (payload) => {
  try {
    const res = 'pending';
  } catch (error) {
    console.error('Error creating user: ', error);
  }
};
